pipeline {
  agent any
  stages {
    stage('Build') {
      steps {
        sh 'docker build -t secure-app .'
      }
    }
    stage('Scan') {
      steps {
        sh 'trivy image secure-app'
      }
    }
  }
}
